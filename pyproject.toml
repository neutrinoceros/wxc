[build-system]
requires = ["setuptools", "wheel"]

[tool.black]
line-length = 80

[tool.isort]
profile = "black"
combine_as_imports = true

[tool.coverage.run]
omit = [
  "setup.py",
  "wxc/__main__.py",
  "install_app.py",
]
